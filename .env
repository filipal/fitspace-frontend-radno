# Base URL for the FitSpace backend REST API.
# Used for authentication handshakes (e.g. POST {apiRoot}/auth/token).
VITE_API_BASE_URL="http://localhost:8080/api"

# Base URL for the avatar API service.
# Must include the trailing "/api/users/" segment so that the frontend
# can append the active user identifier to the request path.
VITE_AVATAR_API_BASE_URL="http://localhost:8080/api/users/"

# Optional API key for the avatar API service (leave blank if not required).
VITE_AVATAR_API_KEY=""