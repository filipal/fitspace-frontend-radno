# Base URL for the FitSpace backend REST API.
# Used for authentication handshakes (e.g. POST {apiRoot}/auth/token).
# VITE_API_BASE_URL="http://localhost:8080/api"
VITE_API_BASE_URL="/api"
# Base URL for the avatar API service.
# Must include the trailing "/api/users/" segment so that the frontend
# can append the active user identifier to the request path.
# VITE_AVATAR_API_BASE_URL="http://localhost:8080/api/users/"
VITE_AVATAR_API_BASE_URL="/api/users/"
# Optional API key for the avatar API service (leave blank if not required).
VITE_AVATAR_API_KEY=""

# Amazon Cognito OAuth konfiguracija.
# Za testiranje na više uređaja koristi isti hostname (npr. preko /etc/hosts)
# i isti URL dodaj u Cognito Allowed callback/sign-out popise.
# Primjer: ako svi uređaji pristupaju preko http://fitspace.local:5177,
# ovdje postavi te URL-ove umjesto localhost.
VITE_COGNITO_DOMAIN="https://your-domain.auth.eu-north-1.amazoncognito.com"
VITE_COGNITO_CLIENT_ID="replace-with-client-id"
VITE_COGNITO_REGION="eu-north-1"
# VITE_COGNITO_REDIRECT_URI="http://localhost:5177/auth/callback"
# VITE_COGNITO_LOGOUT_URI="http://localhost:5177/"

VITE_TUNNEL_HOST=ringtones-becomes-among-age.trycloudflare.com